DONE reader GMSH// :
  [x] (directiry data); gmsh -3 -setnumber partitioner 1 -save_all cube-periodic.geo
  [x] GMSH 4.1 ASCII reader (mesh_reader.cpp)
  [x] Build ParGmshMesh constructor (inheriting from ParMesh) 
  [x] Check with simple examples ex1p.cpp ex3p.cpp and ex4p.cpp, ex8p.cpp
      (shortcuts have been made, to be fixed)
      (building shared vertices data struct in gmsh.cpp is not required ...)
  
Questions :
  - svert_group and svert_list do not seem to be required,
    identify functionnalities that depends on that
  
TODO reader GMSH// :
  [] Finish group_stria and group_squad and group_sedge 
  [] Extensive checks  - few things depends on group_stria/group_squad
      (grep -i FaceIntegrator examples/*p.cpp -> ex8p.cpp to begin with)   
  [] Look at support for high order elements
  [] Clean examples that use ParGmshMesh (directory examples/gmsh) :
      ex3p.cpp, ex1p.cpp, ex4p.cpp, ex8p.cpp and build others  (regression tests)
  [] Reconstruct shared faces/vertices without -save_all
  [] Pb in mesh_readers.cpp during split/partition by GMSH, loosing phys_domain tag
      -> L2689: phys_domain = 99999 // TO BE REMOVED
  [] Write a README with recommandation for using gmsh parallel reader
